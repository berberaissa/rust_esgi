[package]
name = "rust_esgi"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_esgi"
path = "src/lib.rs"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[features]
default = []
